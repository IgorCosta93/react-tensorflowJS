import { connect } from 'react-redux';
import PredictingDiabetesWrapper from "../components/general/PredictingDiabetesWrapper";

const mapStateToProps = state => {
    return {
        
    };
};

const mapDispatchToProps = dispatch => {
    return {
        
    };
};

export default connect(mapStateToProps, mapDispatchToProps)(PredictingDiabetesWrapper);