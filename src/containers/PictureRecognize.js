import { connect } from 'react-redux';
import PictureRecognizeWrapper from "../components/general/PictureRecognizeWrapper";

const mapStateToProps = state => {
    return {
        
    };
};

const mapDispatchToProps = dispatch => {
    return {
        
    };
};

export default connect(mapStateToProps, mapDispatchToProps)(PictureRecognizeWrapper);