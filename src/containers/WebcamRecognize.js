import { connect } from 'react-redux';
import WebcamRecognizeWrapper from "../components/general/WebcamRecognizeWrapper";

const mapStateToProps = state => {
    return {
        
    };
};

const mapDispatchToProps = dispatch => {
    return {
        
    };
};

export default connect(mapStateToProps, mapDispatchToProps)(WebcamRecognizeWrapper);