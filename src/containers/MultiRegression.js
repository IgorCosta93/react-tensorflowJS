import { connect } from 'react-redux';
import MultiRegressionWrapper from "../components/general/MultiRegressionWrapper";

const mapStateToProps = state => {
    return {
        
    };
};

const mapDispatchToProps = dispatch => {
    return {
        
    };
};

export default connect(mapStateToProps, mapDispatchToProps)(MultiRegressionWrapper);