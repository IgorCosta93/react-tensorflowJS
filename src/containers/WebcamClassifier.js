import { connect } from 'react-redux';
import WebcamClassifierWrapper from "../components/general/WebcamClassifierWrapper";

const mapStateToProps = state => {
    return {
        
    };
};

const mapDispatchToProps = dispatch => {
    return {
        
    };
};

export default connect(mapStateToProps, mapDispatchToProps)(WebcamClassifierWrapper);