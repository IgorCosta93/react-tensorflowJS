import { connect } from 'react-redux';
import ImageClassifierWrapper from "../components/general/ImageClassifierWrapper";

const mapStateToProps = state => {
    return {
        
    };
};

const mapDispatchToProps = dispatch => {
    return {
        
    };
};

export default connect(mapStateToProps, mapDispatchToProps)(ImageClassifierWrapper);